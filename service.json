{
  "_comment": "Deployment facts for this project, read by morfdeploy. The four install steps live in one place for the whole parc; this file states only what is specific to this service. Adding a platform never touches this file.",
  "_comment_template": "morfTemplateService est le PATRON dont on clone un nouveau service : il n'est jamais installe en production. 'morf.py install --services' le saute grace a ce drapeau (les autres services ne le portent pas).",
  "template": true,
  "service_name": "morftemplate",
  "display_name": "morfTemplateService",
  "description": "Reference service of the morfSystem ecosystem",
  "binary": "morftemplate",
  "_comment_app_dir": "Where the binary and configurations are installed, per platform. Overridden by the MORF_APP_DIR environment variable, which replaces the per-project prefixes (MT_, MN_, MS_) that had already drifted apart.",
  "app_dir": {
    "linux": "/opt/morftemplate",
    "windows": "%ProgramData%/morftemplate"
  },
  "_comment_configs": "Copied at install. 'overwrite' is false by default because these hold settings edited by hand on the machine. An absolute destination is honoured as-is, which is how the shared parc configuration reaches /etc/morfsystem.",
  "configs": [
    {
      "source": "config/morftemplate.example.json",
      "dest": "morftemplate.json",
      "migrate_from": [
        "/etc/morftemplate/morftemplate.json",
        "/opt/morftemplate/morftemplate.json"
      ]
    }
  ],
  "status_url": "http://127.0.0.1:8901/status",
  "_comment_config_dir": "La configuration vit sous /etc/morfsystem/<service> : un point d'entree UNIQUE dans /etc pour tout le parc, et non a cote du binaire dans /opt. Effacer /opt/<service> pour reinstaller proprement n'emporte donc pas les reglages.",
  "config_dir": {
    "linux": "/etc/morfsystem/morftemplate",
    "windows": "%ProgramData%/morfsystem/morftemplate"
  },
  "_comment_state_dir": "L'ETAT PERSISTANT (cles, coffre, curseurs, sequences de sync, index, derniere execution) vit sous /var/lib/morfsystem/<service> : distinct de la config (/etc, admin, lecture seule) et du programme (/opt). Sous Linux, l'unite declare StateDirectory=morfsystem/<service> : systemd cree ce dossier possede par le user du service. Le service resout sa racine d'etat via $STATE_DIRECTORY (repli /var/lib/morfsystem/<service>). Ne jamais y mettre de config admin, ni mettre d'etat dans /etc (c'est ce qui rendait le coffre morfCollector inecrivable). Sauvegarder /var/lib = sauvegarder tout ce qui est vivant.",
  "state_dir": {
    "linux": "/var/lib/morfsystem/morftemplate",
    "windows": "%ProgramData%/morfsystem/morftemplate/state"
  },
  "_comment_purge": "Catégories de données que CE projet sait effacer, lues par 'service.py purge <id> [--all] [--dry-run]'. Les identifiants sont LIBRES (pas d'énumération figée) : annoncer 'database', 'cache', 'history', 'thumbnails', 'sync-state'... selon le projet. Chaque catégorie porte un label humain et un drapeau 'destructive' (perte réelle de données). Deux types : 'path' (morfdeploy supprime des chemins résolus sous 'base' = state|config|app ; un chemin qui s'échappe de sa base via '..' est refusé) et 'command' (morfdeploy transmet l'intention au binaire du projet, pour une donnée qu'un chemin ne peut pas exprimer, ex. une partie d'une base partagée). Jetons substitués dans une commande : __BINARY__, __STATE_DIR__, __CONFIG_DIR__, __APP_DIR__. Un 'command' n'est simulable sous --dry-run que s'il déclare 'dry_run': true. Bloc facultatif : un service sans 'purge' n'expose simplement aucune catégorie. Exemple ci-dessous, à adapter (le template n'est jamais déployé).",
  "purge": [
    {
      "id": "cache",
      "label": "Cache reconstructible",
      "destructive": false,
      "type": "path",
      "base": "state",
      "paths": ["cache"]
    },
    {
      "id": "database",
      "label": "Base de données du service (irréversible)",
      "destructive": true,
      "type": "path",
      "base": "state",
      "paths": ["morftemplate.db"]
    }
  ]
}
